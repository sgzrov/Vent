{
  "name": "@vent/db",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate": "drizzle-kit generate",
    "migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "^0.30.0",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
