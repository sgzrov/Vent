{
  "name": "@vent/worker",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/bundle.mjs",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vent/shared": "workspace:*",
    "@vent/db": "workspace:*",
    "@vent/platform-connections": "workspace:*",
    "@vent/runner": "workspace:*",
    "@vent/adapters": "workspace:*",
    "bullmq": "catalog:",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "esbuild": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
