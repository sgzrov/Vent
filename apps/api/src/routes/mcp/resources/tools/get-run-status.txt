Get the current status and results of a test run. Blocks until new results arrive — call again immediately after each response.

Use filters to scope results for subagent monitoring:
- `test_type=conversation` + `test_name={name}` — one subagent per conversation test
- `test_type=load_test` — load test results

After all subagents return, call once without filters for the full summary (includes fix_plan and baseline_comparison).